" Add temporary and testing plugins here
" where changes aren't tracked by git.

if has('nvim')
  " Plug 'hen/hardtime.nvim'
endif
if !has('nvim')
endif
if has('gui_running')
endif
if !has('gui_running')
endif
